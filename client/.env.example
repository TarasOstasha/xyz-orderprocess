# Set these in Vercel → Settings → Environment Variables, then Redeploy.
# Demo mode = login + sample orders in the browser (no API required).

VITE_DEMO_MODE=true
VITE_DEMO_USER_NAME=Tony Joss
VITE_DEMO_USER_EMAIL=tonyjoss1990@gmail.com
VITE_DEMO_USER_PASSWORD=xyz333

# Leave unset (or remove localhost) while using demo mode on Vercel
# VITE_API_URL=
